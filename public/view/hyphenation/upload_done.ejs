<!DOCTYPE html>
<html lang="en">

<head>
    <% include ../../partials/head %>
</head>

<body>
    <% include ../../partials/jumbo-header %>

        <div class="container">
            <h4>Following folders were successfully uploaded :</h4>
            <ul class="list-group">
                <% for (var i = 0; i < uploadedList.length; i++) { %>
                    <li class="list-group-item">
                        <% if(!uploadedList[i].isFolder){%>

                            <span><%= uploadedList[i].fileName %></span>
                            <span class="text-danger float-xl-right">Unnecessary file detected! - This file will be ignored</span>
                            <%}else{ %>
                                <span class="font-weight-bold"><%= uploadedList[i].fileName %></span>
                                <%}%>
                    </li>
                    <% } %>
            </ul>


            <h4 class="text-success">Script is now ready to run for the above uploaded folders</h4>

            <a href="execute/">
                <button type="button" class="btn btn-primary">Click to Run</button>
            </a>

        </div>
</body>

</html>